{% extends 'base.html.twig' %}

{% block title %}Films par categorie |
	{{ category.name}}
{% endblock %}

{% block body %}
	<div class='relative w-full'>
		<img src='/uploads/{{category.media.filename}}' alt='category-icon' class='w-full shadow-lg h-[600px] object-cover absolute -top-2 -z-50 opacity:40'>
	</div>

	<div class='flex items-end m-4 justify-end z-50'>
		<div class='lg:w-1/2 mt-6 lg:mt-0 bg-black p-4 rounded-lg bg-opacity-50'>
			<h1 class='text-4xl font-bold leading-tight mb-4 text-white'>{{category.name}}</h1>
			{% if category.featText %}

				<p class='text-lg mb-6'>{{category.featText}}</p>
			{% endif %}
		</div>
	</div>
	{# <div class='lg:max-w-full lg:flex shadow-lg shadow-neutral-800 w-full mx-auto lg:mb-20 md:mb-10'>
						{% if category.featText %}
							<div class='w-full relative'>
								<img src='/uploads/{{category.media.filename}}' alt='category-icon' class='min-w-full h-[250px] object-cover object-center'>
								<div class='lg:absolute top-5 right-5 z-50 bg-black bg-opacity-80 p-4 lg:w-1/3 h-auto'>
									<h1 class='text-yellow-400 text-3xl my-4'>{{category.name}}</h1>
									<p class='text-sm'>{{category.featText}}</p>
								</div>
				
							</div>
						{% else %}
				
							<div class='w-full relative'>
								<img src='/uploads/{{category.media.filename}}' alt='category-icon' class='min-w-full h-[250px] object-cover object-center'>
								<a href='/categorie/{{category.slug}}'>
									<h1 class='text-yellow-400 text-3xl absolute bottom-10 right-10 z-50'>{{category.name}}</h1>
								</a>
							</div>
				
						{% endif %}
					</div> #}

	<div class='grid lg:grid-cols-4 justify-center gap-2 p-4'>
		{% for post in posts %}
			{% include 'post/postCard.html.twig' %}
		{% endfor %}
	</div>
{% endblock %}
